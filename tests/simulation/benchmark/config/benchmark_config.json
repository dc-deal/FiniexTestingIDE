{
    "description": "Benchmark Test Configuration",
    "scenario": "backtesting/backtesting_loadtest_40_scenarios.json",
    "runs": 3,
    "tolerances": {
        "ticks_per_second": {
            "percent": 20.0,
            "description": "Main throughput metric - CPU bound"
        },
        "tickrun_time_s": {
            "percent": 20.0,
            "description": "Tick processing duration - CPU bound"
        },
        "warmup_time_s": {
            "percent": 30.0,
            "description": "Data loading phase - IO bound (disk dependent)"
        }
    },
    "certificate": {
        "validity_days": 90,
        "description": "Benchmark report expires after this many days"
    },
    "failure_hints": {
        "warmup_slow": [
            "Possible causes for slow warmup:",
            "1. Disk change or degradation (HDD vs SSD)",
            "2. Warmup algorithm modified",
            "3. Parquet file structure changed"
        ],
        "tickrun_slow": [
            "Possible causes for slow tick processing:",
            "1. CPU throttling or background load",
            "2. Algorithm regression in worker/decision logic",
            "3. Memory pressure causing swapping"
        ]
    },
    "stability": {
        "max_spread_percent": 15.0,
        "description": "Maximum spread across the raw runs of ticks_per_second. Above this the instrument cannot resolve a code regression, so the median means nothing. Calibrated from the committed reports: worst historical spread 13.0% (1.2.1); the invalid 2026-08-24 runs were 20.2% and 34.4%."
    },
    "ceilings": {
        "summary_generation_time_s": {
            "max_seconds": 10.0,
            "description": "Report DERIVE + PERSIST across the scenario set, once per batch. Was 0.12s through 1.2.x and is ~3.4s since the unified reporting pipeline (#391-#403) \u2014 accepted cost. The ceiling exists so the NEXT growth of that size cannot hide in an untoleranced INFO metric."
        }
    }
}
