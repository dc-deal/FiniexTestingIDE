# Core Data Processing
pandas>=2.0.0
pyarrow>=12.0.0
numpy>=1.24.0
python-dateutil>=2.8.0
tzdata>=2024.1  # IANA timezone DB for stdlib zoneinfo (DST-aware local<->UTC, e.g. swap rollover #365)

# Fast Data Loading
polars>=0.18.0
fastparquet>=2023.4.0

# Development & Debugging
ipython>=8.14.0

# Code Style & Formatting
autopep8>=2.0.0
isort>=5.12.0

# Data Visualization
plotly>=5.15.0
matplotlib>=3.5.0
seaborn>=0.11.0

# HTTP
requests>=2.28.0
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.0.0

# Console
rich>=14.2.0
psutil>=7.1.0

# WebSocket (Kraken WS v2 tick source, #232)
websockets>=12.0
certifi>=2023.7.22

# Testing
pytest>=7.0.0
httpx>=0.27.0

# Static analysis.
# pyflakes — the undefined-name gate (tests/framework/static_analysis/). A name used but
#   never imported is syntactically valid, so only an executed code path reveals it.
# ruff    — unused imports (§7) and placeholder-free f-strings (§5) project-wide, plus the
#   release-time import-order and quote sweep. NOTE: its default quote style is DOUBLE,
#   the opposite of §5 — it must be configured, never run bare.
# vulture — dead code across module boundaries (§19): a getter nobody calls is invisible
#   to pyflakes, which only sees module-local disuse. Needs a whitelist (FastAPI routes and
#   pytest fixtures are called by their frameworks, not by name).
pyflakes>=3.4.0
ruff>=0.16.0
vulture>=2.14
